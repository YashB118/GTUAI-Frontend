// coins/streak feature disabled — restore from git history to re-enable
export default function LeaderboardPage() {
  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <p className="text-sm text-text-muted">Leaderboard is currently disabled.</p>
    </div>
  );
}
