// coins/streak feature disabled — restore from git history to re-enable
export default function AdminChallengesPage() {
  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <p className="text-sm text-text-muted">Challenges feature is currently disabled.</p>
    </div>
  );
}
